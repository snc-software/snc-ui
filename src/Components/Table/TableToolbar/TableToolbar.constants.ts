/**
 * Toolbar actions delegate their appearance to `Button`, so this component defines no variant classes
 * of its own — only which `Button` variant each action defaults to.
 */
export const DefaultLeadingActionVariant = 'primary';
export const DefaultActionVariant = 'secondary';
