import { cn } from '@/Utils/cn';

// Matches `Checkbox`/`Switch`'s own label typography — the one visual treatment for a form label
// already established in this library.
export const classes = {
  base: cn('snc:font-snc-body snc:text-sm snc:text-snc-text-primary'),
} as const;
