/**
 * Page sizes offered by default. Shared by every table layout (for the initial page size) and by
 * `PageSizeSelector`.
 */
export const DefaultPageSizeOptions: number[] = [20, 50, 100];

export const DefaultEmptyMessage = 'No data found';
